#!/bin/sh

# Probably not needed
DEBIAN_FRONTEND=noninteractive
export DEBIAN_FRONTEND

# Installa the packages...
dpkg --force-confdef --force-confold --install packages/*deb || true
# and the dependencies
apt-get install --yes --force-yes --fix-broken && touch /etc/nagios3/nagios-3.5.1.flag

